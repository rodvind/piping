import { ConvMileToKMPipe } from './conv-mile-to-km.pipe';

describe('ConvMileToKMPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvMileToKMPipe();
    expect(pipe).toBeTruthy();
  });
});
